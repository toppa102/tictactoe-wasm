default:
  image: rust:latest

build-everything:
  stage: build
  before_script:
    - apt update && apt -yy install curl npm nodejs
    - curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
  script:
    - wasm-pack build --target web ./tictactoe-rs
    - npm install
    - npm install ./tictactoe-rs/pkg

pages:
  script:
    - npm run build
    - mv dist public
  artifacts:
    paths:
      - public
  only:
    - master
  